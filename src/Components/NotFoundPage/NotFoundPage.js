import React from 'react';

const NotFoundPage = () => {
    return (
        <div style={{minHeight:"75vh"}}>
            <h1>Page Not found</h1>
        </div>
    );
};

export default NotFoundPage;